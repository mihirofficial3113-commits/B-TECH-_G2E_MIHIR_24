#include <stdio.h>
int main(){
    float radius;
    printf("enter the radius of the circle ");
    scanf("%f", &radius);
    printf ("the area of the circle is %f",3.14*radius*radius);
    return 0;
}
