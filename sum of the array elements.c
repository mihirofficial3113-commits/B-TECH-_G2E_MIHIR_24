
#include<stdio.h>
int main(){
    int n, array[n],sum=0,i;
    printf("enter the no. of elements in the array:");
    scanf("%d",&n);
    for(i=0;i<n;i++){
        scanf("%d",&array[i]);
        {
            for(i=0;i<n;i++);
            {
                sum+=array[i];
                printf("the sum of the array is %d",sum);
                return 0;


            }
        }
    }
}